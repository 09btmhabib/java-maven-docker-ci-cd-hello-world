apiVersion: v1
kind: Service
metadata:
  name: {{ include "spring-github-actions.fullname" . }}
  labels:
    {{- include "spring-github-actions.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "spring-github-actions.selectorLabels" . | nindent 4 }}
