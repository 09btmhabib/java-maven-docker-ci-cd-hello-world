apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "spring-github-actions.fullname" . }}-test-connection"
  labels:
    {{- include "spring-github-actions.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "spring-github-actions.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
